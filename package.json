{
	"scripts": {
		"watch": "concurrently \"npx tailwindcss -i ./style.css -o ./src/output.css --watch\" \"live-server ./src cross-env BROWSER=chrome\"",
		"build": "npx tailwindcss -i ./style.css -o ./src/output.css --watch"
	},
	"devDependencies": {
		"tailwindcss": "^3.4.14"
	},
	"dependencies": {
		"concurrently": "^9.0.1",
		"cross-env": "^7.0.3",
		"live-server": "^1.2.2"
	}
}
